import { moduleFor, test } from 'ember-qunit';

moduleFor('adapter:commenter', 'Unit | Adapter | commenter');

// Replace this with your real tests.
test('it exists', function(assert) {
  let adapter = this.subject();
  assert.ok(adapter);
});
